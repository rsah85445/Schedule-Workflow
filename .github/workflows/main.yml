name: Schedule job

on:
  schedule:
     - chron: "0 0 * * *"

     jobs:
       run-script:
         runs-on: ubuntu-latest
         steps:
            - name: Print Message
            run: echo "Schedule Workflow Exeucuted"
